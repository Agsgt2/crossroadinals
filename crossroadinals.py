# Scripts

def crosstart(PID):
  SPID = ''
  if (SPID !== ''):
    print("Replacing PID #",SPID,"to PID #",PID)
    SPID = PID
  else:
    if (PID == SPID):
      print("PID is not changed due to a PID problem")
    else:
      print("PID placed to PID #",PID)
      SPID = PID

